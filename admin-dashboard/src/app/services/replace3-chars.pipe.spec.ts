import { Replace3CharsPipe } from './replace3-chars.pipe';

describe('ReplaceCharsPipe', () => {
  it('create an instance', () => {
    const pipe = new Replace3CharsPipe();
    expect(pipe).toBeTruthy();
  });
});
