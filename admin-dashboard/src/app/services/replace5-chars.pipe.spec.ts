import { Replace5CharsPipe } from './replace5-chars.pipe';

describe('Replace5CharsPipe', () => {
  it('create an instance', () => {
    const pipe = new Replace5CharsPipe();
    expect(pipe).toBeTruthy();
  });
});
